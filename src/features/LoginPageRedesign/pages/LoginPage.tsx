import { AuthLayout, LoginCard } from '@/features/LoginPageRedesign/components';

export const LoginPage = () => {
  return (
    <AuthLayout>
      <LoginCard />
    </AuthLayout>
  );
};

export default LoginPage;
